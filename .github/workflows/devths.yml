name: Devths-FE CICD 파이프라인

on:
  push:
    branches:
      - develop
      - release/*
      - main

env:
  NODE_VERSION: '22.x'
  AWS_REGION: ${{ secrets.AWS_REGION }}
  S3_BUCKET: ${{ github.ref_name == 'main' && secrets.AWS_S3_BUCKET_PROD || secrets.AWS_S3_BUCKET }}
  S3_PREFIX: FE/${{ github.ref_name }}
  CODEDEPLOY_APP_NAME: ${{ secrets.CODEDEPLOY_APP_NAME }}
  CODEDEPLOY_DEPLOYMENT_GROUP: ${{ github.ref_name == 'main' && secrets.CODE_DEPLOY_GROUP_NAME_PROD || (startsWith(github.ref_name, 'release/') && secrets.CODE_DEPLOY_GROUP_NAME_STAGING || secrets.CODE_DEPLOY_GROUP_NAME_DEV) }}

jobs:
  # 1. 통합 품질 검사 (Lint + Prettier + TypeCheck)
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: pnpm 설치
        uses: pnpm/action-setup@v3
        with:
          version: 9 # 프로젝트 환경에 맞는 버전 설정
          run_install: false

      - name: 노드 JS 설치
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 의존성 설치
        run: pnpm install --frozen-lockfile

      - name: 통합 코드 검사
        run: pnpm check # 하나라도 실패하면 여기서 멈춤

  # 2. 정적 분석 (CodeQL)
  static-analysis:
    runs-on: ubuntu-22.04
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: CodeQL 초기화
        uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript
          config-file: './.github/codeql/code-ql-heavy.yml'

      - name: 분석 수행
        uses: github/codeql-action/analyze@v4

      - name: 보안 취약점 결과 확인 및 차단
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "⏳ 분석 결과가 GitHub에 반영되기를 기다리는 중 (10초)..."
          sleep 10

          # 1. 현재 이벤트가 push인지 pr인지에 따라 검색할 REF 설정
          # GITHUB_REF는 push일 땐 refs/heads/branch명, PR일 땐 refs/pull/번호/merge가 됩니다.
          CURRENT_REF="${{ github.ref }}"
          echo "📍 현재 Ref: $CURRENT_REF"

          # 2. API 호출하여 High/Critical 등급의 Open 상태인 알림 조회
          ALERTS=$(gh api "repos/${{ github.repository }}/code-scanning/alerts?tool_name=CodeQL&state=open&ref=$CURRENT_REF" \
            --jq '[.[] | select(.rule.security_severity_level == "high" or .rule.security_severity_level == "critical")]')

          COUNT=$(echo "$ALERTS" | jq 'length')

          if [ "$COUNT" -gt 0 ]; then
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "❌ 보안 검사 실패: 고위험(High/Critical) 취약점 $COUNT개 발견"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "$ALERTS" | jq -r '.[] | "- [\(.rule.security_severity_level)] \(.rule.description) \n   위치: \(.most_recent_instance.location.path)"'
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            exit 1
          else
            echo "✅ 보안 검사 통과: 현재 커밋($CURRENT_REF)은 안전합니다."
          fi

  # 3. 빌드 및 S3 업로드
  build:
    needs: [lint, static-analysis]
    runs-on: ubuntu-22.04
    outputs:
      artifact_name: ${{ steps.upload.outputs.artifact_name }}
      s3_key: ${{ steps.upload.outputs.s3_key }}
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: pnpm 설치
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: 노드 JS 설치
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 의존성 설치
        run: pnpm install --frozen-lockfile

      - name: 환경 변수 설정
        run: |
          if [ "${{ github.ref_name }}" == "main" ]; then
            echo "${{ secrets.ENV_FILE_PROD }}" > .env
          elif [[ "${{ github.ref_name }}" == release/* ]]; then
            echo "${{ secrets.ENV_FILE_STAGING }}" > .env
          else
            echo "${{ secrets.ENV_FILE_DEV }}" > .env
          fi

      - name: 프로젝트 빌드
        run: pnpm build

      - name: AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{secrets.AWS_ACCESS_KEY}}
          aws-secret-access-key: ${{secrets.AWS_SECRET_KEY}}
          aws-region: ${{secrets.AWS_REGION}}

      - name: 타임스탬프 생성
        id: timestamp
        run: |
          # SHA의 앞 7자리만 변수에 저장
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          # 날짜와 SHORT_SHA를 결합하여 OUTPUT에 저장
          echo "value=$(date +'%Y%m%d_%H%M%S')-$SHORT_SHA" >> $GITHUB_OUTPUT

      - name: 배포 아티팩트 생성 및 S3 업로드
        id: upload
        run: |
          # 1. Next.js 일반 빌드 결과물 경로 확인
          if [ ! -d "./.next" ]; then
            echo "❌ .next 빌드 폴더를 찾을 수 없습니다."
            exit 1
          fi

          # 2. 아티팩트 파일명 생성
          ARTIFACT_NAME="${{ steps.timestamp.outputs.value }}.zip"
          echo "artifact_name=$ARTIFACT_NAME" >> $GITHUB_OUTPUT

          # 3. 배포 아티팩트 압축
          echo "🗜️ SSR 배포 아티팩트를 생성합니다: $ARTIFACT_NAME"

          # 임시 디렉토리 생성 및 파일 준비
          mkdir -p deploy-temp

          # CodeDeploy 설정 및 스크립트
          cp appspec.yml deploy-temp/
          cp -r scripts deploy-temp/

          # Next.js 빌드 결과물 (.next 폴더 전체)
          cp -r .next deploy-temp/

          # Public 폴더
          if [ -d "./public" ]; then
            cp -r public deploy-temp/
          fi

          # package.json과 pnpm-lock.yaml (서버에서 의존성 설치용)
          cp package.json deploy-temp/
          cp pnpm-lock.yaml deploy-temp/

          # 환경 변수 파일
          if [ -f ".env" ]; then
            cp .env deploy-temp/
          fi

          # 압축 파일 생성 (node_modules는 제외, 서버에서 설치)
          cd deploy-temp
          zip -r "../$ARTIFACT_NAME" . -q
          cd ..
          rm -rf deploy-temp

          # 4. S3 업로드 경로 설정
          S3_KEY="${S3_PREFIX}/$ARTIFACT_NAME"
          S3_URL="s3://${S3_BUCKET}/${S3_KEY}"
          echo "s3_key=$S3_KEY" >> $GITHUB_OUTPUT

          echo "🚀 $ARTIFACT_NAME을 $S3_URL로 업로드합니다."

          # 5. S3에 압축 파일 업로드
          aws s3 cp "$ARTIFACT_NAME" "$S3_URL" --only-show-errors

          echo "✅ S3 업로드 완료: $S3_URL"
      # --------------------------------

  # 4. CodeDeploy 배포
  deploy:
    needs: [build]
    runs-on: ubuntu-22.04
    # main은 CI까지만 수행 (배포는 별도 운영 프로세스로 진행)
    if: github.ref_name != 'main'
    outputs:
      deployment_id: ${{ steps.create.outputs.deployment_id }}
      deployment_status: ${{ steps.result.outputs.status }}
      deployment_error: ${{ steps.result.outputs.error_message }}
    steps:
      - name: AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: CodeDeploy 배포 시작
        id: create
        run: |
          set -euo pipefail
          echo "🚀 CodeDeploy 배포를 시작합니다..."

          # build job에서 생성된 S3 키 정보가 필요하므로,
          # 여기서는 최신 아티팩트를 찾아 배포합니다
          DEPLOYMENT_ID=$(aws deploy create-deployment \
            --application-name "${{ env.CODEDEPLOY_APP_NAME }}" \
            --deployment-group-name "${{ env.CODEDEPLOY_DEPLOYMENT_GROUP }}" \
            --s3-location bucket="${{ env.S3_BUCKET }}",key="${{ needs.build.outputs.s3_key }}",bundleType=zip \
            --description "Deploy from GitHub Actions - ${{ needs.build.outputs.artifact_name }}" \
            --query 'deploymentId' \
            --output text)
            
          echo "deployment_id=$DEPLOYMENT_ID" >> $GITHUB_OUTPUT
          echo "✅ 배포 ID: $DEPLOYMENT_ID"

      - name: 배포 완료(헬스체크 포함)까지 대기
        id: wait
        run: |
          set -euo pipefail
          DEPLOYMENT_ID="${{ steps.create.outputs.deployment_id }}"
          echo "⏳ 배포 상태를 확인합니다 (ID: $DEPLOYMENT_ID)..."

          # 배포 완료까지 대기 (최대 3분)
          aws deploy wait deployment-successful \
            --deployment-id "$DEPLOYMENT_ID" \
            --cli-read-timeout 180

          echo "✅ CodeDeploy 배포가 성공적으로 완료되었습니다!"

      - name: 배포 결과 조회
        id: result
        if: always()
        run: |
          set -euo pipefail

          DEPLOYMENT_ID="${{ steps.create.outputs.deployment_id }}"
          if [ -z "${DEPLOYMENT_ID:-}" ]; then
            echo "status=UNKNOWN" >> $GITHUB_OUTPUT
            echo "error_message=Failed to create deployment (deployment_id missing)" >> $GITHUB_OUTPUT
            exit 0
          fi

          STATUS=$(aws deploy get-deployment --deployment-id "$DEPLOYMENT_ID" --query 'deploymentInfo.status' --output text)
          ERROR_MESSAGE=$(aws deploy get-deployment --deployment-id "$DEPLOYMENT_ID" --query 'deploymentInfo.errorInformation.message' --output text 2>/dev/null || true)

          if [ "${ERROR_MESSAGE:-}" = "None" ] || [ "${ERROR_MESSAGE:-}" = "null" ]; then
            ERROR_MESSAGE=""
          fi

          ERROR_MESSAGE_CLEAN=$(echo "${ERROR_MESSAGE:-}" | tr '\n' ' ' | head -c 400)

          echo "status=$STATUS" >> $GITHUB_OUTPUT
          echo "error_message=$ERROR_MESSAGE_CLEAN" >> $GITHUB_OUTPUT

          echo "📌 최종 상태: $STATUS"
          if [ -n "$ERROR_MESSAGE_CLEAN" ]; then
            echo "📌 에러: $ERROR_MESSAGE_CLEAN"
          fi

  # 5. CI 디스코드 알림
  notify-ci:
    runs-on: ubuntu-22.04
    needs: [lint, static-analysis, build]
    if: always()
    steps:
      - name: 데이터 준비
        id: prep
        env:
          USER_MAP: ${{ secrets.USER_MAP_JSON }}
        run: |
          # 1. 커밋 메시지 추출 (Push 제목 대용)
          COMMIT_MSG=$(echo "${{ github.event.head_commit.message }}" | head -n 1)
          echo "title=$COMMIT_MSG" >> $GITHUB_OUTPUT

          # 2. 브랜치 정보
          echo "branch=${{ github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Discord 알림 전송
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ github.ref_name == 'main' && secrets.DISCORD_WEBHOOK_PROD_URL || secrets.DISCORD_WEBHOOK_NON_PROD_URL }}
          status: ${{ (needs.lint.result == 'success' && needs.static-analysis.result == 'success' && needs.build.result == 'success') && 'success' || 'failure' }}
          title: "${{ steps.prep.outputs.branch }} CI 결과 [${{ (needs.lint.result == 'success' && needs.static-analysis.result == 'success' && needs.build.result == 'success') && '✅성공' || '❌실패' }}]"
          description: |
            **커밋 메시지**: ${{ steps.prep.outputs.title }}
            **브랜치**: `${{ steps.prep.outputs.branch }}`
            **환경**: ${{ github.ref_name == 'main' && '🚀 운영(PROD)' || (startsWith(github.ref_name, 'release/') && '🧪 스테이징(STAGING)' || '🛠️ 개발(DEV)') }}

            **📊 상세 결과**:
            - Lint/Type: ${{ needs.lint.result == 'success' && ' ✅' || ' ❌' }}
            - Static Analysis: ${{ needs.static-analysis.result == 'success' && ' ✅' || ' ❌' }}
            - Build: ${{ needs.build.result == 'success' && ' ✅' || ' ❌' }}

            **🪣 S3 Key**: `${{ needs.build.outputs.s3_key }}`

            **🔗 링크**: [커밋 확인하기](${{ github.event.repository.html_url }}/commit/${{ github.sha }})

          color: "${{ (needs.lint.result == 'success' && needs.static-analysis.result == 'success' && needs.build.result == 'success') && 65280 || 16711680 }}"
          username: GitHub FE Merge Bot
          avatar_url: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png

  # 6. CD 디스코드 알림 (배포 실패해도 결과 전송)
  notify-cd:
    runs-on: ubuntu-22.04
    needs: [build, deploy]
    if: ${{ github.ref_name != 'main' && needs.build.result == 'success' && always() }}
    steps:
      - name: 데이터 준비
        id: prep
        run: |
          COMMIT_MSG=$(echo "${{ github.event.head_commit.message }}" | head -n 1)
          echo "title=$COMMIT_MSG" >> $GITHUB_OUTPUT
          echo "branch=${{ github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Discord 알림 전송
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ github.ref_name == 'main' && secrets.DISCORD_WEBHOOK_PROD_URL || secrets.DISCORD_WEBHOOK_NON_PROD_URL }}
          status: ${{ needs.deploy.result == 'success' && 'success' || 'failure' }}
          title: "${{ steps.prep.outputs.branch }} CD 결과 [${{ needs.deploy.result == 'success' && '✅성공' || '❌실패' }}]"
          description: |
            **커밋 메시지**: ${{ steps.prep.outputs.title }}
            **브랜치**: `${{ steps.prep.outputs.branch }}`
            **환경**: ${{ startsWith(github.ref_name, 'release/') && '🧪 스테이징(STAGING)' || '🛠️ 개발(DEV)' }}

            **📦 아티팩트**: `${{ needs.build.outputs.artifact_name }}`
            **🪣 S3 Key**: `${{ needs.build.outputs.s3_key }}`
            **🚀 배포 결과**: ${{ needs.deploy.outputs.deployment_status || needs.deploy.result }}
            **🆔 Deployment ID**: `${{ needs.deploy.outputs.deployment_id }}`
            **🧾 에러**: ${{ needs.deploy.outputs.deployment_error || '없음' }}

            **🔗 링크**: [커밋 확인하기](${{ github.event.repository.html_url }}/commit/${{ github.sha }})
          color: "${{ needs.deploy.result == 'success' && 65280 || 16711680 }}"
          username: GitHub FE Deploy Bot
          avatar_url: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png
