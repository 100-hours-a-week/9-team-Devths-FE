name: Devths-FE CICD 파이프라인

on:
  push:
    branches: [develop, release, main]

env:
  NODE_VERSION: '22.x'
  AWS_REGION: ${{ secrets.AWS_REGION }}
  S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
  S3_PREFIX: ${{ github.repository }}/${{ github.ref_name }}/${{ github.sha }}

jobs:
  # 1. 통합 품질 검사 (Lint + Prettier + TypeCheck)
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: pnpm 설치
        uses: pnpm/action-setup@v3
        with:
          version: 9 # 프로젝트 환경에 맞는 버전 설정
          run_install: false

      - name: 노드 JS 설치
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 의존성 설치
        run: pnpm install --frozen-lockfile

      - name: 통합 코드 검사
        run: pnpm check # 하나라도 실패하면 여기서 멈춤

  # 2. 정적 분석 (CodeQL)
  static-analysis:
    runs-on: ubuntu-22.04
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: CodeQL 초기화
        uses: github/codeql-action/init@v3
        with:
          languages: javascript-typescript
      - name: 분석 수행
        uses: github/codeql-action/analyze@v3

  # 3. 빌드 및 S3 업로드
  build:
    needs: [lint, static-analysis]
    runs-on: ubuntu-22.04
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: pnpm 설치
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: 노드 JS 설치
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 의존성 설치
        run: pnpm install --frozen-lockfile

      - name: 프로젝트 빌드
        run: pnpm build

      - name: AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{secrets.AWS_ACCESS_KEY}}
          aws-secret-access-key: ${{secrets.AWS_SECRET_KEY}}
          aws-region: ${{secrets.AWS_REGION}}

      - name: S3에 빌드 결과물 업로드
        run: |
          # 1. Next.js 빌드 결과물 경로 감지
          if [ -d "./.next" ]; then
            SOURCE_DIR="./.next"
          elif [ -d "./out" ]; then
            SOURCE_DIR="./out"
          else
            echo "❌ 빌드 폴더를 찾을 수 없습니다."
            exit 1
          fi

          # 2. S3 최종 경로 설정
          DEST_URL="s3://${{ env.S3_BUCKET }}/${{ env.S3_PREFIX }}"

          echo "🚀 $SOURCE_DIR 폴더의 파일들을 $DEST_URL 경로로 전송합니다."

          # sync 명령어로 로컬과 S3를 동일하게 맞춤
          # --delete 옵션은 로컬에 없는 파일이 S3에 있다면 삭제하여 동기화합니다.
          aws s3 sync $SOURCE_DIR $DEST_URL --delete --only-show-errors
      # --------------------------------

  # 4. 디스코드 통합 알림
  notify-discord:
    runs-on: ubuntu-22.04
    needs: [lint, static-analysis, build]
    if: always()
    steps:
      - name: 데이터 준비
        id: prep
        env:
          USER_MAP: ${{ secrets.USER_MAP_JSON }}
        run: |
          # 1. 커밋 메시지 추출 (Push 제목 대용)
          COMMIT_MSG=$(echo "${{ github.event.head_commit.message }}" | head -n 1)
          echo "title=$COMMIT_MSG" >> $GITHUB_OUTPUT

          # 2. 브랜치 정보
          echo "branch=${{ github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Discord 알림 전송
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ github.ref_name == 'main' && secrets.DISCORD_WEBHOOK_PROD_URL || secrets.DISCORD_WEBHOOK_NON_PROD_URL }}
          status: ${{ (needs.lint.result == 'success' && needs.static-analysis.result == 'success' && needs.build.result == 'success') && 'success' || 'failure' }}
          title: "${{ steps.prep.outputs.branch }} 결과 [${{ (needs.lint.result == 'success' && needs.static-analysis.result == 'success' && needs.build.result == 'success') && '✅성공' || '❌실패' }}]"
          description: |
            **커밋 메시지**: ${{ steps.prep.outputs.title }}
            **브랜치**: `${{ steps.prep.outputs.branch }}`
            **환경**: ${{ github.ref_name == 'main' && '🚀 운영(PROD)' || (github.ref_name == 'release' && '🧪 스테이징(STAGING)' || '🛠️ 개발(DEV)') }}
            
            **📊 상세 결과**:
            - Lint/Type: ${{ needs.lint.result == 'success' && ' ✅' || ' ❌' }}
            - Static Analysis: ${{ needs.static-analysis.result == 'success' && ' ✅' || ' ❌' }}
            - Build: ${{ needs.build.result == 'success' && ' ✅' || ' ❌' }}
            
            **🔗 링크**: [커밋 확인하기](${{ github.event.repository.html_url }}/commit/${{ github.sha }})

          color: "${{ (needs.lint.result == 'success' && needs.static-analysis.result == 'success' && needs.build.result == 'success') && 65280 || 16711680 }}"
          username: GitHub FE Merge Bot
          avatar_url: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png
